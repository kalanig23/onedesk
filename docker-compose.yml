services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: onedesk
      POSTGRES_PASSWORD: onedesk
      POSTGRES_DB: onedesk
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: